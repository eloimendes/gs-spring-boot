@Field def releaseNumber

node {
   def mvnHome
   stage('Preparation') { // for display purposes
      // Get some code from a GitHub repository
      git branch: 'docker-multi-stage', url: 'https://github.com/eloimendes/gs-spring-boot.git'
   }
   stage('Build jar') {
      // Run the maven build
      dir("complete") {
        sh 'chmod +x mvn-docker.sh'
        sh './mvn-docker.sh'
        releaseNumber=readFile('target/release').trim()
      }
   }
   stage('Build image') {
      // Build docker image
      dir("complete") {
        sh 'docker build -t eloi-docker-multi/gs-spring-boot:${releaseNumber} .'
      }
   }
}